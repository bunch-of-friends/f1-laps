use pipeline::types::*;

pub fn store_metadata(
    input_tick: &InputTick,
    context: &Context,
    labels: &PacketLabels,
    stats: &PacketStats,
    store_lap_result: &StoreLapResult
) -> StoreMetadataResult {
    StoreMetadataResult {}
}
