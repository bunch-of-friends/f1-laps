use pipeline::types::*;

pub fn store_metadata(
    input_tick: &InputTick,
    context: &Context,
    labels: &Labels,
    stats: &Stats,
    store_lap_result: &StoreLapResult
) -> StoreMetadataResult {
    StoreMetadataResult {}
}
